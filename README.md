# Duplicate-File-Finder
"A fast, Python-based duplicate file finder with Hebrew support, smart hashing for large videos, and a safe 'Send to Trash' feature."

## Installation & Setup

To run this project, you need Python installed. It is recommended to use a virtual environment (VENV).

1. **Clone the repository:**
   ```bash
   git clone [https://github.com/Aslisiko/Duplicate-File-Finder.git](https://github.com/Aslisiko/Duplicate-File-Finder.git)
   cd Duplicate-File-Finder
   
